#!/usr/bin/env ruby
require 'irb'
require "irb/completion"

require_relative "../urna"

# init db connection
Urna::Db::init(:config=>'config/database.yml',:env=>'development')


trap("SIGINT") do
  IRB.abort
  #irb.signal_handle
end

#catch(:IRB_EXIT) do
#  irb.eval_input
#end


IRB.start
